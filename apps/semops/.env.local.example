# Supabase Configuration (Local Development)
# Copy this file to .env.local and fill in values from `supabase status`

# Local Supabase URL (default port 54321)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Anonymous key from `supabase status`
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key from `supabase status` (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Production Configuration (Vercel Environment Variables)
# These are set in Vercel dashboard, not in local files:
# - NEXT_PUBLIC_SUPABASE_URL: Your Supabase cloud project URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY: Your Supabase cloud anon key
# - SUPABASE_SERVICE_ROLE_KEY: Your Supabase cloud service role key
